# AGENTS.md — KVDB SDK 的 AI 工作守则

> 本文件是 AI 在本项目中工作的**唯一入口与最高规范**。任何 AI 会话开始前必须先读本文件,
> 再读 `docs/nextsession.md`(当前进度),然后才开始工作。
>
> 核心理念:**为 AI 的可理解性而优化,而非为人类的优雅而优化。**
> 目标是让 AI 能在有限上下文内可靠地理解、修改、扩展本系统,并跨多次会话稳定演进。

---

## 0. 项目一句话定位

KVDB SDK 是一个面向 Node.js 的**高性能、多驱动、可扩展**的 KV Database SDK:
统一数据接口(SQLite / MongoDB / PostgreSQL,后续 Redis / MySQL)+ JSON 存储与查询 + 独立 Cache 系统 + 装饰器缓存,完整 TypeScript 支持。

完整愿景见 `docs/SPEC.md`。架构见 `docs/ARCHITECTURE.md`。

---

## 1. 执行协议(严格)

每开始任何一步之前,AI **必须**:

1. **明确声明当前处于哪一步**
2. **说明本步将产出什么**
3. **严格按下面的步骤顺序执行**

### 步骤工作流

| 步骤 | 名称 | 产出 | 是否允许写实现代码 |
|---|---|---|---|
| Step 1 | 架构设计(强制最先) | 系统架构 / 模块拆分 / 数据流 / 关键决策 | ❌ 否 |
| Step 2 | 文档 | `docs/SPEC.md` / `docs/BUILD.md` | ❌ 否 |
| Step 3 | 上下文交接 | `docs/nextsession.md` | ❌ 否 |
| Step 4 | 实现 | 代码、模块 | ✅ 仅在用户显式批准后 |

> 当前状态:**Step 1–3 已完成,等待用户批准进入 Step 4。** 见 `docs/nextsession.md`。

### 自我纠正规则

一旦发现以下任一情况,**停下并重构,而不是继续往下写**:

- 过早编码(architecture / 契约还没定就写实现)
- 模块拆分不合理
- 复杂度在上升(嵌套加深、函数职责膨胀)

---

## 2. 架构原则(面向 AI)

1. **认知驱动拆分(最重要)**:模块按"AI 能否孤立理解"来拆,**不**按行数/文件大小。
2. **单一职责**:每个模块只做一件事,有清晰的 input/output,无隐藏依赖。
3. **局部可理解**:不读整个项目也能理解一个模块。避免逻辑散落多文件、跨文件依赖链。
4. **命名即文档**:命名描述性、显式、反映用途。禁止 `cfg` / `tmp` / `svc` 这类缩写。
5. **仅显式行为**:禁止隐藏副作用、魔法逻辑、隐式状态变更。所有行为从代码本身可见。
6. **复杂度控制**:简单控制流、可预测逻辑。避免深嵌套、多用途函数。
7. **组合优于继承**:继承引入隐藏行为,AI 难以推理。
8. **清晰入口**:定义 main entry、配置入口、模块边界。
9. **显式依赖**:所有依赖显式 import、来源可见。禁止全局隐藏状态、隐式注入。
10. **增量可构建**:支持逐步开发,每步可独立测试。

### AI 专属约束

- **文件级可理解**:每个文件能被孤立理解。
- **无隐藏上下文**:不要求记住多个文件、不依赖隐藏假设。
- **受控信息密度**:单文件不堆叠多层抽象、不写超载函数。
- **清晰数据流**:数据从哪来、到哪去,必须显式。
- **可预测行为**:相同输入产生一致输出,最小化副作用。

---

## 3. 反模式(必须避免)

- 巨大、难懂的模块
- "上帝" 工具文件(`utils.ts` 什么都往里塞)
- 过度抽象 / 投机式抽象(为假想的后端镀金)
- 深继承链
- 隐藏配置
- 逻辑散落多文件
- **最低公分母陷阱**:只统一所有后端都支持的功能,导致能力被阉割 → 用能力标志(capabilities)+ 原生逃生通道(raw)代替
- **自己实现连接池**:池属于底层 driver,透传配置即可
- **SQL 字符串往返**:从 IR 直接生成 SQL,不要解析再重发 SQL

---

## 4. 配置策略

配置**非强制**。若使用:必须集中、有清晰 schema/types、不得散落在代码各处。

---

## 5. Git 工作流

每完成一个主要步骤后:

```bash
git add .
git commit -m "feat: <描述当前步骤>"
```

❗ **不要 push**(除非用户显式要求)。提交信息结尾保留:
`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

---

## 6. 文档地图(AI 查找指引)

| 我想知道… | 看这个文件 |
|---|---|
| 怎么和 AI 协作 / 协议 / 原则 | `AGENTS.md`(本文件) |
| 系统架构、模块边界、数据流、关键技术决策 | `docs/ARCHITECTURE.md` |
| 公开 API、数据模型、契约、行为规格 | `docs/SPEC.md` |
| 怎么构建、安装、运行、测试 | `docs/BUILD.md` |
| 当前进度、已完成、下一步、风险 | `docs/nextsession.md` |
| 依赖/对接项目的官方文档地址 | `docs/EXTERNAL-DOCS.md` |

---

## 7. 技术基线(本项目已确认)

- Node.js v24+(开发机 v24.2),pnpm 10.x,TypeScript 5.8。
- 包管理器:**pnpm**。
- 模块格式:ESM(`"type": "module"`),同时产出 ESM + CJS + `.d.ts`(由 tsup/构建工具)。
- 装饰器:**TC39 标准装饰器**(TS 5.x 原生),**不**用 `experimentalDecorators`。
- SQLite 后端:现阶段用 `better-sqlite3`;`node:sqlite`(2026 仍为 RC)作为版本门控的可选适配器。

> 任何会改变上述基线的决定,必须先更新本节并在 `docs/nextsession.md` 记录理由。
